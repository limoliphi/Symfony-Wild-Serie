{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block title %}Catégorie{% endblock %}

{% block body %}

    <div class="m-5 container">
        <h1>Pour ajouter un nom de catégorie, c'est ici...</h1>
    </div>
    <div class="m-5 container">

    {{ form_start(form) }}

    {{ form_row(form.name) }}

    <button class="btn btn-danger">{{ button_label|default('Valider son choix') }}</button>

    {{ form_end(form) }}
    </div>
    <div class="m-5 container">
        <a href="/wild">
            <button type="button" class="btn btn-primary">Revenir aux séries</button>
        </a>
    </div>
    </div>

{% endblock %}
